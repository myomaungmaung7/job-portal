spring:
  datasource:
    url: jdbc:mysql://localhost/job_portal?allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: mysql

  #Mail Configuration
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: c8553006479278
    password: 6f2e863aa4961f
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
 secret: MyNewLocalSecretKeyForJobPortal2026!