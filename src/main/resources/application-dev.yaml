spring:
  datasource:
    url: jdbc:mysql://localhost/job_portal?allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: mysql

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  #Mail Configuration
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 7a8d1afb0f79ff
    password: 6003d9d4f7dcd7
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  file:
    path:
      profile:
        absolutePath: C:/projectImages/profileImages
        relativePath: /image/user/profile/
      nrcFront:
        absolutePath: C:/projectImages/nrcFrontImages
        relativePath: /image/user/nrc/front/
      nrcBack:
        absolutePath: C:/projectImages/nrcBackImages
        relativePath: /image/user/nrc/back/
      cvForm:
        absolutePath: C:/projectPdf/cvForm
        relativePath: /pdf/user/cv

jwt:
 secret: MyNewLocalSecretKeyForJobPortal2026!